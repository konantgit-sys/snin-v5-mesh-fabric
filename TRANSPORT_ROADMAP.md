# 🪜 SNIN Transport Roadmap — от TCP до Erlang

> Создано: 2026-07-23 17:20 MSK
> Контекст: объяснение Антону уровней межагентного транспорта
> Статус: Уровень 1 (production), Уровень 2 (groundwork laid, не активирован)

---

## Суть за две минуты

SNIN использует TCP JSON-line для общения агентов (5600 msg/s). Это работает для 3 агентов, но не масштабируется. Мы заложили фундамент под ZeroMQ (уровень 2, 50-120K msg/s), и понимаем дорожную карту до Erlang/OTP (уровень 5, 20M msg/s).

Ключевой принцип: **каждый следующий уровень активируется ТОЛЬКО когда текущий перестаёт справляться.** Не строим Kafka для трёх агентов.

---

## Полная лестница

| Уровень | Технология | msg/s (1 ядро) | Latency | Зависимости | Кто использует |
|:---:|---|---:|---:|---|---|
| **1. Сырой TCP** | `socket()` + JSON | 5 600 | 0.1-0.3ms | Нет | Наш текущий код |
| **2. Умные сокеты** | ZeroMQ | 50K-120K | 0.05-0.2ms | `libzmq3-dev` + `pyzmq` | HFT-трейдинг, игровые серверы |
| **3. Брокер сообщений** | NATS | 2 000K | 0.1-0.5ms | Бинарник NATS (20MB RAM) | Cloud Foundry, Synadia |
| **4. Событийная шина** | Apache Kafka | 5 000K | 2-10ms | JVM, ZooKeeper/KRaft | Uber, Netflix, банки |
| **5. Actor-модель** | Erlang/OTP | 20 000K | микросекунды | Полный рантайм BEAM | WhatsApp, Ericsson |
| **6. Kernel bypass** | DPDK / RDMA | 100 000K+ | 1-2μs | Свои сетевые карты | Телеком, биржи |

---

## Где мы сейчас — и куда идём

```
Уровень 1 ████████████░░░░░░░░  TCP JSON-line — PRODUCTION, 5600 msg/s
Уровень 2 ████████░░░░░░░░░░░░  ZeroMQ — GROUNDWORK LAID, не активирован
Уровень 3 ░░░░░░░░░░░░░░░░░░░░  NATS — когда 50+ агентов
Уровень 4 ░░░░░░░░░░░░░░░░░░░░  Kafka — коммерческий продукт
Уровень 5 ░░░░░░░░░░░░░░░░░░░░  Erlang/OTP — переписывание ядра
Уровень 6 ░░░░░░░░░░░░░░░░░░░░  DPDK — никогда
```

### Уровень 1: TCP JSON-line (СЕЙЧАС)

**Файл:** `hybrid/hybrid_channel.py`, `smart_router.py`
**Порты:** :9970 (HCOOR), :45083 (Cryter), :37845 (Forecaster), :46721 (Archivist)
**Протокол:** JSON-строки через raw TCP

Почему этого хватает:
- 3 агента × 1 событие/сек = 3 msg/s. Запас в 1866 раз.
- 0 зависимостей (голый Python)
- Легко дебажить (`nc 127.0.0.1 9970`)
- Легко тестировать

Когда перестанет хватать:
- Агентов станет >20
- SmartRouter latency превысит 10ms
- Появятся дропы сообщений под нагрузкой

### Уровень 2: ZeroMQ (ЗАЛОЖЕН, НЕ АКТИВИРОВАН)

**Файл:** `zmq_transport.py` (487 строк)
**Коммит:** `28aed64` — feat: ZeroMQ transport groundwork
**Порты зарезервированы:** :9960 (ROUTER), :9961 (PUB), :9962 (PUSH), :9963 (SUB proxy)
**Флаг активации:** `export SNIN_USE_ZMQ=1`

4 паттерна, 4 класса:
- `ZmqRouter` (ROUTER/DEALER) — замена p2p_msg→ACK
- `ZmqPublisher` (PUB/SUB) — замена gossip loop
- `ZmqPipeline` (PUSH/PULL) — параллельные воркеры
- `ZmqSubscriber` (SUB) — прокси для NAT

Что даст активация:
- 50 000 msg/s (ROUTER/DEALER) — в 9× быстрее TCP
- 120 000 msg/s (PUB/SUB) — broadcast за 0.5ms вместо 5 секунд
- Автоматическая балансировка между воркерами
- Агенты за NAT могут слушать через SUB proxy

Что нужно для активации:
```bash
sudo apt-get install libzmq3-dev
pip3 install pyzmq --break-system-packages
export SNIN_USE_ZMQ=1
# Перезапустить SmartRouter
```

Почему НЕ активировали сейчас:
- 3 агентам не нужен throughput 50K msg/s
- Добавляет C-зависимость (libzmq3-dev)
- Усложняет дебаг (нет `nc` для ZMQ — нужен `zmq_recv`)
- TCP JSON-line справляется с запасом в 56×

### Уровень 3: NATS (КОГДА 50+ АГЕНТОВ)

**Когда:** агентов >50 И SmartRouter latency >10ms И дропы сообщений >0.1%

Что даст:
- 2 000 000 msg/s на ядро
- Встроенная персистентность (JetStream)
- Кластеризация из коробки
- WebSocket-мост для браузерных клиентов
- Нативный Python-клиент (`nats-py`)

Что потребует:
- Отдельный процесс NATS-сервера (20MB RAM)
- Переписать SmartRouter.send() на NATS-субъекты
- ~1000 строк конфигурации/интеграции
- 2-3 дня работы

### Уровень 4: Kafka (КОММЕРЧЕСКИЙ ПРОДУКТ)

**Когда:** нужно хранить ВСЮ историю сообщений, replay, аналитика на больших данных.

Почему не раньше:
- JVM жрёт 1-2 GB RAM минимум
- Администрирование сложное (ZooKeeper или KRaft)
- Функции (replay, партиции) не нужны для AI-агентов
- Оверхед: Kafka спроектирована для миллионов событий/сек с хранением на годы

### Уровень 5: Erlang/OTP (ПЕРЕПИСЫВАНИЕ ЯДРА)

**Когда:** Python становится bottleneck'ом на уровне языка.

Что даст:
- 20 000 000 msg/s на ядро
- Location transparency (агент не знает где физически запущен другой агент)
- Supervision trees (упавший агент перезапускается автоматически)
- Hot-swap кода без остановки системы
- 2M+ TCP-соединений на одном сервере

Что потребует:
- Переписать всё ядро SNIN на Erlang/Elixir
- Новый рантайм (BEAM)
- 2+ месяца работы
- Мост между Python-агентами и Erlang-ядром

**Решение «переписать на Erlang» принимается только когда Python ДОКАЗАННО является bottleneck'ом — не раньше.**

### Уровень 6: DPDK/RDMA (НИКОГДА)

Не для нас. Используется биржами где микросекунда = миллион долларов.
Требует управления сетевыми картами на уровне железа — невозможно в контейнере.

---

## Критерии перехода между уровнями

| Текущий → Следующий | Триггер | Метрика |
|---|---|---|
| TCP → ZeroMQ | Агентов >20 | SmartRouter latency >10ms |
| ZeroMQ → NATS | Агентов >50 | Дропы сообщений >0.1% |
| NATS → Kafka | Хранение истории >1TB | Нужен replay + аналитика |
| Kafka → Erlang | Python bottleneck | CPU >80% на маршрутизации |

---

## Ресурсы на каждом уровне

| Уровень | RAM (доп.) | CPU (доп.) | Диск | Сложность дебага |
|---|---|---|---|---|
| TCP JSON-line | 2MB | 0.1% | 0 | Тривиально (`nc`) |
| ZeroMQ | 5MB + libzmq | 0.2% | 0 | Нужен zmq-сниффер |
| NATS | 20MB | 1% | 100MB (JetStream) | NATS CLI |
| Kafka | 1-2GB (JVM) | 5% | 10GB+ | Kafka Tools |
| Erlang | 50MB (BEAM) | 0.5% | 0 | Observer, tracing |

---

## История решений

- **2026-07-22:** Обсудили уровни транспорта. Антон спросил «что выше ZeroMQ?»
- **2026-07-23:** Заложили фундамент ZeroMQ — `zmq_transport.py` (487 строк), интеграция в SmartRouter
- **2026-07-23:** Создан этот документ — полная дорожная карта транспорта

---

## Связанные файлы

| Файл | Что |
|---|---|
| `zmq_transport.py` | 4 класса ZeroMQ (ROUTER, PUB, PUSH, SUB) |
| `smart_router.py` | Интеграция — импорт, channel_health, __init__ |
| `hybrid/hybrid_channel.py` | Текущий транспорт (TCP JSON-line) |
| `SNIN_KNOWLEDGE_BASE_DEEP.md` | Карта всех слоёв и портов |
| `SNIN_KNOWLEDGE_BASE.md` | Процессы, директории, БД |
